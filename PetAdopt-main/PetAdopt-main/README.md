# PetAdopt

Project đã được chỉnh để frontend gọi backend thật qua:
- Frontend: `http://localhost:5173`
- Backend API: `http://localhost:5000/api`

## File cấu hình đã thêm
- `backend/.env`
- `backend/.env.example`
- `frontend/.env`
- `frontend/.env.example`

## Tài liệu chạy project
Xem chi tiết tại:
- `HUONG_DAN_CHAY.md`
